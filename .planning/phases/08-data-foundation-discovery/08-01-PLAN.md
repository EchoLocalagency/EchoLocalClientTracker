---
phase: 08-data-foundation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/add_directory_system_tables.sql
  - src/lib/types.ts
  - scripts/seo_engine/seed_client_profiles.py
  - scripts/seo_engine/seed_directories.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "client_profiles table exists in Supabase with all NAP and extended profile fields"
    - "directories table exists with tier, trade, CAPTCHA status, DA score, and UNIQUE(domain)"
    - "submissions table exists with UNIQUE(client_id, directory_id) and status workflow"
    - "All 4 active clients have been seeded into client_profiles from clients.json"
    - "55 directories are seeded into directories table with tier, trade tags, and submission URLs"
  artifacts:
    - path: "supabase/migrations/add_directory_system_tables.sql"
      provides: "Schema for client_profiles, directories, submissions tables"
      contains: "UNIQUE(client_id, directory_id)"
    - path: "src/lib/types.ts"
      provides: "ClientProfile, Directory, Submission TypeScript interfaces"
      exports: ["ClientProfile", "Directory", "Submission"]
    - path: "scripts/seo_engine/seed_client_profiles.py"
      provides: "One-time seed from clients.json to client_profiles table"
    - path: "scripts/seo_engine/seed_directories.py"
      provides: "Seed 55 directories with tier, trade, DA, submission URL"
  key_links:
    - from: "scripts/seo_engine/seed_client_profiles.py"
      to: "client_profiles table"
      via: "supabase-py upsert"
      pattern: "supabase.*client_profiles.*upsert"
    - from: "scripts/seo_engine/seed_directories.py"
      to: "directories table"
      via: "supabase-py upsert"
      pattern: "supabase.*directories.*upsert"
    - from: "src/lib/types.ts"
      to: "Supabase schema"
      via: "interface fields match table columns"
---

<objective>
Create the three core Supabase tables (client_profiles, directories, submissions), add TypeScript interfaces, and seed both tables with real data -- 4 active clients from clients.json and 55 directories with tier/trade/DA metadata.

Purpose: This is the data foundation that every subsequent phase depends on. No submission engine, verification loop, or dashboard can function without these tables and seed data.
Output: Migration SQL executed, TypeScript types exported, 4 client profiles and 55 directories seeded in Supabase.
</objective>

<execution_context>
@/Users/brianegan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brianegan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-foundation-discovery/08-CONTEXT.md
@.planning/phases/08-data-foundation-discovery/08-RESEARCH.md

@clients.json
@scripts/seo_engine/backlinks/directory_audit.py
@src/lib/types.ts
@supabase/migrations/add_seo_engine_columns.sql

<interfaces>
<!-- Existing Client interface from src/lib/types.ts that seed script must read -->
From src/lib/types.ts:
```typescript
export interface Client {
  id: string;
  name: string;
  slug: string;
  phone: string | null;
  website: string | null;
  service_areas: string[] | null;
  primary_market: string | null;
  // ... other operational fields
}
```

<!-- Existing directory list from directory_audit.py to extend -->
From scripts/seo_engine/backlinks/directory_audit.py:
- Contains 15 hardcoded directories (Yelp, BBB, Angi, HomeAdvisor, Thumbtack, Houzz, Manta, etc.)
- Uses Brave Search site: query pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration SQL, run it, and add TypeScript interfaces</name>
  <files>supabase/migrations/add_directory_system_tables.sql, src/lib/types.ts</files>
  <action>
1. Create `supabase/migrations/add_directory_system_tables.sql` with three tables following the exact schema from 08-RESEARCH.md Code Examples section:
   - `client_profiles`: UUID PK, client_id FK with ON DELETE CASCADE and UNIQUE(client_id), all NAP fields (business_name, phone, address_street/city/state/zip, email, website), extended fields (description, short_description, services[], certifications[], payment_methods[], hours jsonb, social_links jsonb, year_established, logo_url), timestamps
   - `directories`: UUID PK, name, domain with UNIQUE(domain), submission_url, tier (integer default 3), trades text[], submission_method, captcha_status (default 'unknown'), captcha_checked_at, da_score, enabled (default true), notes, timestamps
   - `submissions`: UUID PK, client_id FK with CASCADE, directory_id FK with CASCADE, UNIQUE(client_id, directory_id), status (default 'pending'), live_url, submitted_at, verified_at, notes, metadata jsonb, timestamps
   - All indexes from the research schema (client_profiles_client, directories_tier, directories_trades GIN, submissions_client, submissions_directory, submissions_status)

2. Run the migration against Supabase using the existing pattern. Check how `add_seo_engine_columns.sql` was applied (likely via Supabase dashboard SQL editor or psql). Use the same method -- run via `psql` or the Supabase CLI if available, otherwise provide instructions.

3. Append TypeScript interfaces to `src/lib/types.ts` (do NOT replace existing content). Add `ClientProfile`, `Directory`, and `Submission` interfaces matching the exact schema from 08-RESEARCH.md TypeScript section. Place them after the existing interfaces.

IMPORTANT: Follow the existing schema pattern from supabase_migration_v3_backlinks.sql -- UUID PKs, ON DELETE CASCADE on all client_id FKs, CREATE TABLE IF NOT EXISTS, CREATE INDEX IF NOT EXISTS.
  </action>
  <verify>
    <automated>cd /Users/brianegan/EchoLocalClientTracker && grep -c "ClientProfile\|Directory\|Submission" src/lib/types.ts | grep -q "[3-9]" && echo "Types added" || echo "FAIL: Missing types"</automated>
  </verify>
  <done>Migration SQL file exists with all three tables, indexes, and constraints. TypeScript interfaces for ClientProfile, Directory, Submission are exported from types.ts. Tables exist in Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Create and run seed scripts for client profiles and 55 directories</name>
  <files>scripts/seo_engine/seed_client_profiles.py, scripts/seo_engine/seed_directories.py</files>
  <action>
1. Create `scripts/seo_engine/seed_client_profiles.py`:
   - Read `clients.json` to get all 4 active clients
   - For each client, build a client_profiles row: map name -> business_name, phone -> phone, website -> website, primary_market -> address_city (parse city from market string), service_areas stays on clients table
   - For same_as_urls in clients.json: create submission rows with status='existing_needs_review' for any directory URLs that match a known directory domain (e.g., yelp.com link -> find the yelp directory row, create submission linking client to that directory)
   - Use supabase-py upsert with on_conflict='client_id' so the script is idempotent
   - Use the existing `_get_supabase()` pattern from other scripts in scripts/seo_engine/
   - Print summary: "Seeded N client profiles, created M existing listing records"

2. Create `scripts/seo_engine/seed_directories.py`:
   - Start with the 15 directories from directory_audit.py (Yelp, BBB, Angi/HomeAdvisor, Thumbtack, Houzz, Manta, etc.)
   - Add ~40 more directories to reach 55 total. Research the actual directory landscape for home services. Include a mix of:
     * Universal directories: CitySearch, Superpages, YellowPages, Foursquare, MapQuest, Apple Maps Connect, Bing Places, Nextdoor, Alignable, Merchant Circle, EZLocal, Local.com, ShowMeLocal, USCity.net, BrownBook.net, Spoke, Cylex, iBegin, Tupalo, Fyple
     * Trade-specific (turf/landscaping/pressure washing): LawnStarter, GreenPal, Lawn Love, TurfMutt, PressureWashingResource, PowerWashFinder, FindAContractor, GuildQuality, ProMatcher, Bark
     * Industry aggregators: Expertise.com, UpCity, Clutch (for service businesses), Birdeye, Podium, NiceJob
   - Each directory needs: name, domain, submission_url (best known URL), tier (1/2/3 per hybrid tier definition from research), trades[] (empty for universal, specific tags for trade-specific), da_score (approximate), submission_method ('web_form' for most, 'api' or 'manual' where known)
   - Use tier definitions: Tier 1 = DA 50+, requires verification/accreditation (BBB, Angi, Houzz). Tier 2 = DA 30-50, needs review/approval (Thumbtack, Expertise, UpCity). Tier 3 = DA 10-30, open registration (Manta, Hotfrog, etc.)
   - captcha_status defaults to 'unknown' -- the CAPTCHA audit happens in Plan 03
   - Use upsert with on_conflict='domain' so the script is idempotent
   - Print summary: "Seeded N directories (Tier 1: X, Tier 2: Y, Tier 3: Z)"

3. Run both scripts in order (profiles first, then directories, then re-run profiles to create the existing_needs_review submission rows since directory IDs are now available).

IMPORTANT: Use dotenv for env loading. Use the existing Supabase client pattern from other scripts in the seo_engine folder.
  </action>
  <verify>
    <automated>cd /Users/brianegan/EchoLocalClientTracker && python3 -c "from scripts.seo_engine.seed_client_profiles import *; print('profiles importable')" && python3 -c "from scripts.seo_engine.seed_directories import *; print('directories importable')" && echo "Both seed scripts importable"</automated>
  </verify>
  <done>4 client profiles seeded in Supabase from clients.json data. 55 directories seeded with tier, trade, DA, and submission URL. Existing listings from same_as_urls created as existing_needs_review submission rows. Both scripts are idempotent (can be re-run safely).</done>
</task>

</tasks>

<verification>
1. Query Supabase: `SELECT count(*) FROM client_profiles` returns 4
2. Query Supabase: `SELECT count(*) FROM directories` returns ~55
3. Query Supabase: `SELECT count(*) FROM submissions WHERE status='existing_needs_review'` returns > 0
4. Attempt duplicate insert: `INSERT INTO submissions (client_id, directory_id, status) VALUES (existing_client, existing_dir, 'pending')` should fail with UNIQUE violation
5. TypeScript types compile: `npx tsc --noEmit src/lib/types.ts` passes
</verification>

<success_criteria>
- Three tables exist in Supabase with correct columns, constraints, and indexes
- UNIQUE(client_id, directory_id) on submissions prevents duplicates at DB level
- 4 client profiles populated with NAP data from clients.json
- ~55 directories seeded with tier, trade tags, DA scores, submission URLs
- Existing listings from same_as_urls appear as existing_needs_review submissions
- ClientProfile, Directory, Submission interfaces exported from types.ts
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-foundation-discovery/08-01-SUMMARY.md`
</output>
