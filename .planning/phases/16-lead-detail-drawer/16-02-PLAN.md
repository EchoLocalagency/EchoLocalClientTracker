---
phase: 16-lead-detail-drawer
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/components/pipeline/LeadChecklist.tsx
  - src/components/pipeline/CommsLog.tsx
  - src/app/pipeline/page.tsx
  - src/components/pipeline/LeadDrawer.tsx
autonomous: true
requirements: [DETAIL-03, DETAIL-04]

must_haves:
  truths:
    - "Drawer shows stage-specific checklist items that admin can check/uncheck"
    - "Checklist completion state persists per-lead in pipeline_checklist_items via Supabase upsert"
    - "Leads entering a new stage see default checklist items (unchecked) even before any DB rows exist"
    - "Admin can add a communication log entry with type, notes, and optional occurred_at from the drawer"
    - "New comm entry appears immediately in the timeline without page refresh"
    - "Clicking a lead row in the pipeline table opens the drawer"
    - "Stage dropdown click does not also open the drawer (stopPropagation)"
    - "Pipeline table updates when drawer closes after edits"
  artifacts:
    - path: "src/components/pipeline/LeadChecklist.tsx"
      provides: "Stage-specific checklist with optimistic toggle and Supabase upsert"
      exports: ["LeadChecklist"]
    - path: "src/components/pipeline/CommsLog.tsx"
      provides: "Communication timeline and add-entry form"
      exports: ["CommsLog"]
    - path: "src/app/pipeline/page.tsx"
      provides: "Updated pipeline page with selectedLeadId state, row onClick, LeadDrawer integration"
    - path: "src/components/pipeline/LeadDrawer.tsx"
      provides: "Updated drawer with LeadChecklist and CommsLog replacing placeholders"
  key_links:
    - from: "src/components/pipeline/LeadChecklist.tsx"
      to: "supabase"
      via: "supabase.from('pipeline_checklist_items').upsert() with onConflict: 'lead_id,stage,item_key'"
      pattern: "upsert.*onConflict"
    - from: "src/components/pipeline/CommsLog.tsx"
      to: "supabase"
      via: "supabase.from('pipeline_comms').insert() with optimistic prepend"
      pattern: "pipeline_comms.*insert"
    - from: "src/app/pipeline/page.tsx"
      to: "src/components/pipeline/LeadDrawer.tsx"
      via: "selectedLeadId state passed as leadId prop, row onClick sets it"
      pattern: "setSelectedLeadId"
---

<objective>
Build the checklist and comms log drawer sections, then wire the drawer into the pipeline page.

Purpose: Completes the drawer with the two interactive sections (checklist toggle, comms log entry) and makes the drawer accessible by clicking any lead row in the pipeline table.
Output: Fully functional lead detail drawer integrated into the pipeline page.
</objective>

<execution_context>
@/Users/brianegan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brianegan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-lead-detail-drawer/16-RESEARCH.md
@.planning/phases/16-lead-detail-drawer/16-01-SUMMARY.md

<interfaces>
<!-- From plan 16-01 outputs -->

From src/components/pipeline/LeadDrawer.tsx (created in 16-01):
```typescript
// Props: { leadId: string | null; onClose: () => void; onLeadUpdated: (lead: PipelineLead) => void }
// Internal state: lead, history, checklistItems, comms, loading
// Has placeholder divs for Checklist and CommsLog sections that need to be replaced
```

From src/lib/types.ts:
```typescript
export interface PipelineChecklistItem {
  id: string; lead_id: string; stage: PipelineStage;
  item_key: string; item_label: string;
  completed: boolean; completed_at: string | null; created_at: string;
}
export interface PipelineComm {
  id: string; lead_id: string; comm_type: CommType;
  direction: CommDirection; notes: string | null;
  occurred_at: string; created_at: string;
}
export type CommType = 'call' | 'email' | 'text';
```

From src/lib/pipeline-constants.ts:
```typescript
export const STAGE_CHECKLIST_DEFAULTS: Record<PipelineStage, Array<{ key: string; label: string }>>;
```

From src/app/pipeline/page.tsx (current state):
- Has: leads state, setLeads, checklistProgress state, lastContact state, changeStage function
- Needs: selectedLeadId state, row onClick handler, LeadDrawer import + render, stopPropagation on stage select td
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LeadChecklist and CommsLog components</name>
  <files>src/components/pipeline/LeadChecklist.tsx, src/components/pipeline/CommsLog.tsx</files>
  <action>
Create `src/components/pipeline/LeadChecklist.tsx` ('use client'):
- Props: `{ lead: PipelineLead; items: PipelineChecklistItem[]; setItems: React.Dispatch<React.SetStateAction<PipelineChecklistItem[]>> }`.
- Section heading: "Checklist" using fontSize 14, fontWeight 600, color var(--text-primary), marginBottom 12, marginTop 24.
- Merge DB items with defaults using the pattern from research:
  - Get `STAGE_CHECKLIST_DEFAULTS[lead.stage]` for current stage defaults.
  - Build a map of DB items by item_key (filter items to current stage only).
  - For each default, use the DB row if it exists, otherwise synthesize a local item with `completed: false`, id `synthetic-{key}`.
- Render each merged item as a flex row (gap 10, alignItems center, padding 6px 0):
  - Checkbox input (width 16, height 16, cursor pointer, accentColor var(--accent)).
  - Label text (fontSize 13, color var(--text-primary), cursor pointer).
  - Completed items: label gets textDecoration line-through, color var(--text-secondary).
- Toggle handler (`toggleItem`):
  - Optimistic: flip completed in local items state via setItems.
  - Supabase upsert: `supabase.from('pipeline_checklist_items').upsert({ lead_id: lead.id, stage: lead.stage, item_key, item_label, completed: newValue, completed_at: newValue ? new Date().toISOString() : null }, { onConflict: 'lead_id,stage,item_key' })`.
  - On error: revert the optimistic toggle, console.error.
- Show progress text below checklist: "{done}/{total} complete" in fontSize 11, color var(--text-secondary), marginTop 8.

Create `src/components/pipeline/CommsLog.tsx` ('use client'):
- Props: `{ leadId: string; comms: PipelineComm[]; setComms: React.Dispatch<React.SetStateAction<PipelineComm[]>> }`.
- Section heading: "Communications" using fontSize 14, fontWeight 600, color var(--text-primary), marginBottom 12, marginTop 24.
- Add-entry form at top of section (before the timeline):
  - Row 1: comm_type select (call/email/text) styled like pipeline page selects (bg var(--bg-depth), border 1px solid var(--border), borderRadius 6, padding 4px 8px, fontSize 12, fontFamily var(--font-mono), color var(--text-primary)).
  - Row 2: notes textarea (3 rows, width 100%, same input styling as LeadProfile).
  - Row 3: occurred_at input type="datetime-local" (default to current local datetime, same input styling). Format default value: `new Date().toISOString().slice(0, 16)`.
  - Row 4: "Add Entry" button (background var(--accent), color var(--bg-surface), border none, borderRadius 6, padding 8px 16px, fontSize 12, fontWeight 600, cursor pointer).
- State: `commType` (default 'call'), `commNotes` (default ''), `commDate` (default current local datetime string).
- Submit handler (`addComm`):
  - Create optimistic entry with `crypto.randomUUID()` as id, direction 'outbound'.
  - Prepend to comms array via setComms.
  - Reset form fields.
  - Insert to Supabase: `supabase.from('pipeline_comms').insert({ lead_id: leadId, comm_type, direction: 'outbound', notes: commNotes || null, occurred_at: new Date(commDate).toISOString() }).select().single()`.
  - On success: replace optimistic entry with real DB row (for correct id).
  - On error: remove optimistic entry from array, console.error.
- Timeline below form: map over comms (already sorted desc by occurred_at):
  - Each entry: flex row, gap 10, padding 8px 0, borderBottom 1px solid var(--border).
  - Left: comm_type badge (fontSize 10, fontFamily var(--font-mono), textTransform uppercase, padding 2px 6px, borderRadius 4, background var(--bg-depth), color var(--text-secondary)).
  - Right column:
    - Notes text (fontSize 13, color var(--text-primary)). If no notes, show "No notes" in var(--text-secondary).
    - Date: `new Date(comm.occurred_at).toLocaleString()` (fontSize 11, color var(--text-secondary), marginTop 2).
  </action>
  <verify>
    <automated>cd /Users/brianegan/EchoLocalClientTracker && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>LeadChecklist renders stage-specific items with check/uncheck persisted via Supabase upsert. CommsLog shows timeline and add-entry form with optimistic insert. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire drawer into pipeline page and replace placeholder sections</name>
  <files>src/components/pipeline/LeadDrawer.tsx, src/app/pipeline/page.tsx</files>
  <action>
Update `src/components/pipeline/LeadDrawer.tsx`:
- Import LeadChecklist and CommsLog components.
- Replace the Checklist placeholder div with: `<LeadChecklist lead={lead} items={checklistItems} setItems={setChecklistItems} />`.
- Replace the Communications placeholder div with: `<CommsLog leadId={lead.id} comms={comms} setComms={setComms} />`.
- No other changes to the drawer.

Update `src/app/pipeline/page.tsx`:
- Add import: `import { LeadDrawer } from '@/components/pipeline/LeadDrawer'`.
- Add state: `const [selectedLeadId, setSelectedLeadId] = useState<string | null>(null)`.
- Add onClick to each `<tr>`: `onClick={() => setSelectedLeadId(lead.id)}`, add `cursor: 'pointer'` to tr style.
- Add stopPropagation to the stage select's parent `<td>`: `onClick={e => e.stopPropagation()}`.
- Add `<LeadDrawer>` after the closing `</div>` of the table container (but inside the return):
  ```
  <LeadDrawer
    leadId={selectedLeadId}
    onClose={() => setSelectedLeadId(null)}
    onLeadUpdated={(updated) => {
      setLeads(prev => prev.map(l => l.id === updated.id ? updated : l));
    }}
  />
  ```
- The onLeadUpdated callback ensures the table row reflects any profile edits made in the drawer.
  </action>
  <verify>
    <automated>cd /Users/brianegan/EchoLocalClientTracker && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Clicking a lead row opens the drawer. Stage dropdown does not trigger drawer open. Drawer shows profile, stage timeline, checklist, and comms log. Pipeline table reflects edits on drawer close. Production build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. All component files exist under src/components/pipeline/
3. Pipeline page imports and renders LeadDrawer with selectedLeadId state
4. Stage select td has stopPropagation to prevent row click conflict
5. LeadChecklist uses upsert with onConflict for checklist persistence
6. CommsLog form inserts entries with optimistic UI
</verification>

<success_criteria>
- Full production build passes
- Clicking a lead row opens the detail drawer
- Stage dropdown click does not open drawer (stopPropagation)
- Checklist shows current-stage items with check/uncheck saved via Supabase upsert
- Default checklist items appear for leads with no DB checklist rows
- Comms log shows existing entries and allows adding new ones with optimistic insert
- Editing profile fields in drawer updates the pipeline table when drawer is used
</success_criteria>

<output>
After completion, create `.planning/phases/16-lead-detail-drawer/16-02-SUMMARY.md`
</output>
