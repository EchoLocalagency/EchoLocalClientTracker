---
phase: 15-page-shell-pipeline-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Admin user sees a 'Pipeline' link in the sidebar engine-links section"
    - "Non-admin user does not see the Pipeline link"
    - "Clicking the Pipeline link navigates to /pipeline"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "Pipeline engine link, conditionally rendered for admin"
      contains: "pipeline"
    - path: "src/app/page.tsx"
      provides: "Passes isAdmin prop to Sidebar"
      contains: "isAdmin"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/Sidebar.tsx"
      via: "isAdmin prop"
      pattern: "isAdmin.*Sidebar"
---

<objective>
Add an admin-only "Pipeline" link to the Sidebar component.

Purpose: UI-01 requires a top-level sidebar link visible only to admin users that navigates to /pipeline.
Output: Modified Sidebar.tsx with conditional Pipeline link, modified page.tsx to pass isAdmin prop.
</objective>

<execution_context>
@/Users/brianegan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brianegan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-page-shell-pipeline-table/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/Sidebar.tsx:
```typescript
interface SidebarProps {
  clients: Client[];
  activeClient: Client | null;
  onSelectClient: (client: Client) => void;
  collapsed: boolean;
  onToggle: () => void;
  onSignOut?: () => void;
}
```

From src/app/page.tsx (how Sidebar is rendered):
```typescript
const { profile, loading: authLoading, isAdmin } = useAuth();
// ...
{isAdmin && (
  <Sidebar
    clients={clients}
    activeClient={activeClient}
    onSelectClient={(c) => { setActiveClient(c); setActiveTab('overview'); }}
    collapsed={sidebarCollapsed}
    onToggle={() => setSidebarCollapsed(!sidebarCollapsed)}
    onSignOut={async () => { await supabase.auth.signOut(); window.location.href = '/login'; }}
  />
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isAdmin prop to Sidebar and render conditional Pipeline link</name>
  <files>src/components/Sidebar.tsx, src/app/page.tsx</files>
  <action>
1. In `src/components/Sidebar.tsx`:
   - Add `isAdmin?: boolean` to `SidebarProps` interface (optional so existing non-admin usage doesn't break)
   - Add destructured `isAdmin` to the component function params
   - In the engine-links div (the section with Agents, SEO Engine, Sales Engine links), add a Pipeline link BEFORE the Agents link, conditionally rendered with `{isAdmin && (...)}`. The link follows the exact same pattern as the other engine links:
     ```
     href="/pipeline"
     color: '#A78BFA' (purple -- distinct from orange/green/teal)
     hover background: 'rgba(167, 139, 250, 0.08)'
     collapsed text: 'PL'
     expanded text: 'Pipeline >'  (use \u203A for >)
     ```
   - All other styles match existing engine links exactly: display flex, alignItems center, gap 10, padding conditional on collapsed, fontSize 12, fontWeight 600, fontFamily var(--font-mono), textDecoration none, textTransform uppercase, letterSpacing 0.05em, transition all 0.15s ease

2. In `src/app/page.tsx`:
   - Add `isAdmin={isAdmin}` prop to the Sidebar component where it's rendered (around line 536)
  </action>
  <verify>
    <automated>cd /Users/brianegan/EchoLocalClientTracker && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Pipeline link appears in sidebar for admin users only. Non-admin users do not see it. Link navigates to /pipeline. Purple color (#A78BFA) is distinct from other engine link colors.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no TypeScript errors
- Sidebar.tsx contains conditional Pipeline link with isAdmin guard
- page.tsx passes isAdmin to Sidebar
</verification>

<success_criteria>
Admin sees purple "Pipeline" link in sidebar engine-links section. Non-admin does not see it. Clicking navigates to /pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/15-page-shell-pipeline-table/15-01-SUMMARY.md`
</output>
