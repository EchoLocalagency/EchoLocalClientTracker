---
phase: 09-submission-engine
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scripts/seo_engine/submission_engine.py
  - scripts/seo_engine/form_configs/overrides/hotfrog.py
  - scripts/seo_engine/form_configs/overrides/manta.py
  - scripts/seo_engine/form_configs/overrides/citysquares.py
autonomous: false
requirements: [SUB-01, SUB-03, SUB-05]

must_haves:
  truths:
    - "Dry-run output shows every eligible Tier 3 no-CAPTCHA directory with its form config status"
    - "Directories where semantic locators fail have per-directory override configs created"
    - "Brian has reviewed dry-run output and confirmed the engine is ready for live submissions"
  artifacts:
    - path: "scripts/seo_engine/submission_engine.py"
      provides: "Polished engine with any fixes from dry-run testing"
    - path: "scripts/seo_engine/form_configs/overrides/"
      provides: "Per-directory config overrides for non-standard forms"
  key_links:
    - from: "scripts/seo_engine/form_configs/overrides/"
      to: "scripts/seo_engine/form_configs/__init__.py"
      via: "get_form_config() loads override when available"
      pattern: "import.*overrides"
---

<objective>
Dry-run the submission engine against all eligible directories to identify which need per-directory form config overrides, create those overrides, and get Brian's sign-off before any live submissions.

Purpose: The research estimates ~70% of directories work with generic semantic locators. This plan identifies and fixes the other 30% so live submissions succeed.
Output: Per-directory override configs for non-standard forms, and Brian's confirmation that dry-run output looks correct.
</objective>

<execution_context>
@/Users/brianegan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brianegan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-submission-engine/09-RESEARCH.md
@.planning/phases/09-submission-engine/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From scripts/seo_engine/submission_engine.py:
```python
async def run_submission_engine(client_slug: str, dry_run: bool = False, run_limit: int = 5)
def check_rate_limits(sb, client_id: str) -> tuple
def nap_audit(profile: dict, form_data: dict) -> list
class SubmissionStage(Enum): FORM_LOADED, FORM_FILLED, POST_SENT
```

From scripts/seo_engine/form_configs/__init__.py:
```python
def get_form_config(directory_domain: str) -> dict
# Returns: {"business_name": [{"type": "label", "value": "Business Name"}, ...], ...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run against all eligible directories and create overrides for failures</name>
  <files>scripts/seo_engine/submission_engine.py, scripts/seo_engine/form_configs/overrides/hotfrog.py, scripts/seo_engine/form_configs/overrides/manta.py, scripts/seo_engine/form_configs/overrides/citysquares.py</files>
  <action>
1. Run the engine in dry-run mode for one client to see all eligible directories:
   ```
   python3 -m scripts.seo_engine.submission_engine --client-slug mr-green-turf-clean --dry-run
   ```

2. Review the dry-run output. For each eligible directory, the engine should show:
   - Directory name, domain, tier, captcha_status
   - Whether a submission already exists (and its status)
   - Current rate limit counts
   - Form config source (base or override)

3. If dry-run reveals issues (missing fields in output, query errors, form config loading problems), fix them in submission_engine.py.

4. For directories where the base form config is known to have non-standard field names (from 09-RESEARCH.md: hotfrog, manta, citysquares are listed as likely needing overrides), create per-directory override configs in `scripts/seo_engine/form_configs/overrides/`:
   - Each override file exports a `FIELD_MAP` dict with the same structure as base_config but with directory-specific locator strategies
   - Only override fields that differ from base -- the loader should merge base + override (override wins)
   - Visit each directory's submission URL in a browser to identify the actual form field labels/placeholders/selectors
   - If a directory submission URL is broken or requires account creation, note it in the override file and flag it for manual handling

5. Run dry-run again to confirm all overrides load correctly.

Note: Only create override files for directories where you can verify the actual form structure. Do not guess at field names. If a directory requires account creation to access the submission form, note this in the override file as a comment and set a flag like `REQUIRES_ACCOUNT = True` so the engine can skip it.
  </action>
  <verify>
    <automated>python3 -m scripts.seo_engine.submission_engine --client-slug mr-green-turf-clean --dry-run 2>&1 | tail -20</automated>
  </verify>
  <done>Dry-run completes without errors for at least one client. Per-directory overrides created for directories with non-standard forms. Engine correctly loads overrides and falls back to base config.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Brian reviews dry-run output and approves engine for live use</name>
  <action>
Brian reviews the submission engine dry-run output to confirm:
1. The correct set of Tier 3 no-CAPTCHA directories are listed
2. Rate limit counts are accurate
3. No unexpected directories are included or missing
4. Directories requiring account creation are properly flagged

Run: `python3 -m scripts.seo_engine.submission_engine --client-slug mr-green-turf-clean --dry-run`
  </action>
  <verify>Brian confirms output is correct and engine is ready for live submissions</verify>
  <done>Brian has approved the engine. It is safe to run without --dry-run for live directory submissions.</done>
</task>

</tasks>

<verification>
1. `python3 -m scripts.seo_engine.submission_engine --client-slug mr-green-turf-clean --dry-run` completes without errors
2. Override configs load for any directories that need them
3. Brian confirms dry-run output is correct
</verification>

<success_criteria>
- Dry-run output lists all eligible directories with correct status information
- Per-directory overrides exist for directories with non-standard forms
- Brian has reviewed and approved the engine for live use
- No directories are silently skipped without explanation in the output
</success_criteria>

<output>
After completion, create `.planning/phases/09-submission-engine/09-02-SUMMARY.md`
</output>
