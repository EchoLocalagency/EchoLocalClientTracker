---
phase: 02-geo-scoring-ai-overview-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seo_engine/serpapi_client.py
  - scripts/seo_engine/research/serp_scraper.py
  - scripts/seo_engine/research/research_runner.py
  - supabase_migration_serp_features.sql
autonomous: true
requirements: [SERP-05, SERP-06, SERP-07, SERP-08, SERP-09]

must_haves:
  truths:
    - "Each tracked keyword records whether it triggers an AI Overview"
    - "Client URL citation in AI Overview references is detected and stored"
    - "Two-step AI Overview fetch with page_token happens immediately per keyword (within 60s window)"
    - "PAA questions are captured per keyword in Supabase"
    - "Featured Snippet holder is tracked per keyword"
  artifacts:
    - path: "supabase_migration_serp_features.sql"
      provides: "serp_features table DDL with indexes"
      contains: "CREATE TABLE"
    - path: "scripts/seo_engine/serpapi_client.py"
      provides: "fetch_ai_overview() function for two-step page_token fetch"
      exports: ["fetch_ai_overview"]
    - path: "scripts/seo_engine/research/serp_scraper.py"
      provides: "Two-step AI Overview fetch inline in keyword loop"
      contains: "fetch_ai_overview"
    - path: "scripts/seo_engine/research/research_runner.py"
      provides: "process_serp_features() call after scrape_serp"
      contains: "process_serp_features"
  key_links:
    - from: "scripts/seo_engine/research/serp_scraper.py"
      to: "scripts/seo_engine/serpapi_client.py"
      via: "import fetch_ai_overview"
      pattern: "from scripts.seo_engine.serpapi_client import.*fetch_ai_overview"
    - from: "scripts/seo_engine/research/research_runner.py"
      to: "supabase serp_features table"
      via: "process_serp_features inserts per-keyword rows"
      pattern: 'sb\.table\("serp_features"\)'
---

<objective>
Extract AI Overview, PAA, and Featured Snippet data from SerpAPI responses and store per-keyword SERP features in Supabase for time-series tracking.

Purpose: Establishes the SERP feature data layer that tells the brain which keywords trigger AI Overviews, whether the client is cited, who holds Featured Snippets, and what PAA questions exist. This is the "detection" half of Phase 2.

Output: serp_features Supabase table populated on every research run with AI Overview, PAA, and Featured Snippet data per keyword.
</objective>

<execution_context>
@/Users/brianegan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brianegan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-geo-scoring-ai-overview-detection/02-RESEARCH.md
@.planning/phases/01-serpapi-foundation/01-01-SUMMARY.md
@.planning/phases/01-serpapi-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key functions from serpapi_client.py that this plan extends -->
From scripts/seo_engine/serpapi_client.py:
```python
def _get_supabase():  # Returns Supabase client
def check_budget(client_id: str) -> dict:  # {"allowed": bool, "reason": str, ...}
def search_google(query: str, client_id: str, location: str) -> dict:  # Raw SerpAPI response
def format_organic_results(serpapi_results: dict) -> list:  # Legacy format converter
def check_account_balance() -> dict:  # Account API (free)
```

From scripts/seo_engine/research/serp_scraper.py:
```python
def resolve_location(short_location: str) -> str:
def scrape_serp(keywords, location=..., max_results=10, client_id=None) -> tuple:
    # Returns (organic_results, raw_extras)
    # raw_extras[kw] = {"ai_overview": ..., "related_questions": [...], "answer_box": ...}
```

From scripts/seo_engine/research/research_runner.py:
```python
def run_research(client_config, force=False):
    # Line 92: organic, serp_extras = scrape_serp(serp_keywords, location=location, client_id=client_id)
    # Line 94: cache["serp_extras"] = serp_extras
```

Client config fields:
- client_config["_supabase_id"] -- UUID for client
- client_config["website"] -- e.g. "https://mrgreenturfclean.com/"
- client_config["slug"] -- e.g. "mr-green-turf-clean"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create serp_features table and add fetch_ai_overview to serpapi_client</name>
  <files>supabase_migration_serp_features.sql, scripts/seo_engine/serpapi_client.py</files>
  <action>
1. Create `supabase_migration_serp_features.sql` with the serp_features table DDL from the research doc (copy exactly -- id UUID PK, client_id UUID FK, keyword TEXT, has_ai_overview BOOLEAN, client_cited_in_ai_overview BOOLEAN, ai_overview_references JSONB, paa_questions JSONB, paa_data JSONB, has_featured_snippet BOOLEAN, featured_snippet_holder TEXT, client_has_snippet BOOLEAN, collected_at TIMESTAMPTZ). Include both indexes (idx_serp_features_keyword and idx_serp_features_ai_overview).

2. Run the migration against Supabase using the Management API pattern from Plan 01-01 (POST /v1/projects/{ref}/database/query with SUPABASE_ACCESS_TOKEN). The project ref is extracted from SUPABASE_URL env var.

3. Add `fetch_ai_overview(page_token: str, client_id: str) -> dict` to `serpapi_client.py`:
   - Call check_budget(client_id) first -- return {"blocked": True, "reason": ...} if budget exceeded
   - Use GoogleSearch with engine="google_ai_overview" and the page_token
   - Log to serpapi_usage with search_type="ai_overview" and query="ai_overview_followup"
   - Return the full response dict
   - Add fetch_ai_overview to the module's imports in the try/except block at the top

4. Add `url_matches_client(url: str, client_website: str) -> bool` helper to serpapi_client.py using urllib.parse to normalize domains (strip www., compare netloc). This is used by research_runner for citation checks.
  </action>
  <verify>
    <automated>python3 -c "from scripts.seo_engine.serpapi_client import fetch_ai_overview, url_matches_client; print('imports ok'); assert url_matches_client('https://www.mrgreenturfclean.com/services', 'https://mrgreenturfclean.com/') == True; assert url_matches_client('https://competitor.com', 'https://mrgreenturfclean.com/') == False; print('url matching ok')"</automated>
  </verify>
  <done>serp_features table exists in Supabase. fetch_ai_overview and url_matches_client are importable and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Wire two-step AI Overview into scrape_serp and add SERP feature processing to research_runner</name>
  <files>scripts/seo_engine/research/serp_scraper.py, scripts/seo_engine/research/research_runner.py</files>
  <action>
1. Modify `scrape_serp()` in serp_scraper.py to handle two-step AI Overview fetch INLINE in the keyword loop. After getting the initial result for each keyword:
   - Check if result.get("ai_overview", {}).get("page_token") exists
   - If yes, immediately call fetch_ai_overview(page_token, client_id) -- import it at the top alongside search_google and format_organic_results
   - If the follow-up succeeds (not blocked, no error), replace ai_overview in raw_extras with the full response's ai_overview
   - This MUST happen before moving to the next keyword (60-second token expiry)
   - If fetch fails or is budget-blocked, keep the original partial ai_overview data

2. Add `process_serp_features(serp_extras: dict, client_id: str, client_website: str)` function to research_runner.py:
   - Import url_matches_client from serpapi_client
   - Import json (already imported)
   - For each keyword in serp_extras, extract:
     - has_ai_overview: bool(ai_overview and not ai_overview.get("error"))
     - client_cited_in_ai_overview: check each reference link with url_matches_client
     - ai_overview_references: json.dumps(references list)
     - paa_questions: json.dumps([q.get("question","") for q in related_questions])
     - paa_data: json.dumps(related_questions)
     - has_featured_snippet: bool(answer_box)
     - featured_snippet_holder: answer_box.get("link", "") if answer_box else ""
     - client_has_snippet: url_matches_client(snippet_holder, client_website)
   - Insert each row into serp_features table via Supabase
   - Log success/failure count

3. Call process_serp_features() in run_research() right after the scrape_serp() call (after line 94 in current code). Pass serp_extras, client_id, and client_config["website"]. Wrap in try/except so failures don't block the rest of research.
  </action>
  <verify>
    <automated>python3 -c "from scripts.seo_engine.research.serp_scraper import scrape_serp; from scripts.seo_engine.research.research_runner import process_serp_features; print('imports ok')"</automated>
  </verify>
  <done>Two-step AI Overview fetch fires immediately per keyword in scrape_serp. process_serp_features stores AI Overview detection, citation status, PAA questions, and Featured Snippet data per keyword in Supabase serp_features table after every research run.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from scripts.seo_engine.serpapi_client import fetch_ai_overview, url_matches_client"` -- imports work
2. `python3 -c "from scripts.seo_engine.research.research_runner import process_serp_features"` -- function exists
3. serp_features table queryable: `python3 -c "from supabase import create_client; import os; sb = create_client(os.getenv('SUPABASE_URL'), os.getenv('SUPABASE_KEY')); print(sb.table('serp_features').select('id').limit(1).execute())"` returns without error
</verification>

<success_criteria>
- serp_features table exists in Supabase with correct schema and indexes
- fetch_ai_overview function handles budget gate and logs usage as search_type="ai_overview"
- scrape_serp handles page_token immediately per keyword (not deferred)
- process_serp_features stores per-keyword SERP feature data after every research run
- url_matches_client correctly handles www/non-www domain variations
</success_criteria>

<output>
After completion, create `.planning/phases/02-geo-scoring-ai-overview-detection/02-01-SUMMARY.md`
</output>
